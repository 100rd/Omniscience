# golangci-lint configuration for omniscience-operator.
#
# Aligns roughly with kubebuilder defaults, with a few additions that match
# the standards in CLAUDE.md (no magic constants, single responsibility).
#
# v1 schema — compatible with golangci-lint >=1.50 (CI runs 1.61).

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gocritic
    - gosec

linters-settings:
  revive:
    rules:
      - name: exported
        arguments:
          - "checkPrivateReceivers"
          - "disableStutteringCheck"
  gosec:
    excludes:
      # G104: errors not checked — handled per-call by errcheck above.
      - G104

issues:
  max-same-issues: 0
  max-issues-per-linter: 0
